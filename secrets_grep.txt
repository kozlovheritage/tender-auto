17:    def _me_patch(self, url, proxies, stream, verify, cert):
23:    def _rq_patch(self, *a, **k):
118:def _patched_session_request(self, method, url, **kw):
176:def _load_secrets():
196:    secrets_path = _FROZEN_BASE / "secrets.txt"
199:            print("[ПРЕДУПРЕЖДЕНИЕ] secrets.txt не найден и переменные окружения не заданы.")
226:def _find_excel_file(verbose=False):
294:def _is_reliable_brand(brand_hint: str) -> bool:
550:def init_decisions_db():
578:def _load_checkpoint():
590:def _save_checkpoint(data):
597:def _delete_checkpoint():
608:def append_history_xlsx(new_records):
693:def save_run_summary(results, output_dir, path_mapping=None):
809:def safe_filename(name, max_len=120):
816:def decode_content_disposition(header_val):
863:def parse_date_from_excel(date_val):
915:def _fz_kind(reg_number):
922:def _parse_source_url(url):
934:def _extract_notice_guid(html):
960:def _eis_get(session, url, **kwargs):
971:def _fetch_guid_via_api(reg_number, notice_type, session):
1011:def _find_guid_via_search(reg_number, session):
1043:def fetch_notice_guid(reg_number, session, source_url=None):
1108:def _fetch_guid_fz44(reg_number, session):
1161:def _find_fz223_documents_url(reg_number, session):
1215:def get_file_links_from_documents_page(reg_number, notice_guid, session):
1281:def _extract_download_url_from_html(html_text, uid, page_url):
1316:def download_file(url, uid, link_text, save_dir, session, retries=5, referer=None, _net_err=None):
1455:def download_tender_documents(reg_number, row, session, tender_dir):
1518:def extract_text_from_doc(filepath):
1584:def extract_text_from_docx(filepath):
1611:def extract_text_from_file(filepath):
1761:def _detect_extension(filepath):
1820:def _extract_pdf(filepath):
1914:def _extract_odt(filepath):
1919:        def _collect_text(node):
1948:def _extract_ods(filepath):
1961:                        def _collect(node, parts):
1993:def _extract_pptx(filepath):
2033:def _extract_image_ocr(filepath, ext):
2076:def extract_relevant_sections(full_text, first_chars=15000):
2108:def is_critical_file(filename, content, success_files):
2120:def extract_texts_from_paths(downloaded_paths, max_total=None, max_per_file=None):
2191:def extract_hints_from_text(text):
2330:def extract_manufacturer_from_chunks(doc_text, subject_str=""):
2388:def validate_manufacturer_for_category(brand_str, subject_str=""):
2422:def lookup_brand_country(brand_hint):
2477:def call_deepseek(prompt):
2561:def _has_self_contradiction(text):
2566:def _has_stopfactor_triggered(text):
2573:def classify_deepseek_response(response):
2628:def extract_rejection_reason(response):
2643:def extract_china_flag(response):
2653:def clean_response_preamble(response):
2685:def postprocess_response(response, combined_hints):
2709:    def _fix_nds_rate(m):
2718:def python_validate_term(response):
2754:def analyze_response_completeness(response_text):
2788:def check_stopfactors_preprocess(excel_subject):
3061:def _log_decision(reg_number, response, extra_note=""):
3071:def run_post_validations(
3513:def _translate_subject_for_search(subject: str) -> str:
3562:def _clean_subject_for_search(subject: str) -> str:
3622:def langsearch_search(query, max_results=10):
3666:def _web_search(query, max_results=10):
3688:def _generate_suppliers_llm(subject, brand=None, china_available=False):
3799:def _clean_raw_email(raw):
3822:def _decode_cfemail(encoded):
3831:def _extract_emails_from_text(text):
3846:def _is_b2p_domain(url):
3861:def _is_safe_host(hostname: str) -> bool:
3881:def _safe_get_public(url, _max_hops=3):
3899:def _scrape_contact_emails(domains, max_requests=SCRAPE_MAX_REQUESTS, max_inflight=SCRAPE_MAX_INFLIGHT):
3920:    def _claim_slot():
3927:    def _refund_slot():
3931:    def _safe_get(url, _max_hops=3):
3955:    def _fetch_domain(domain):
4012:def validate_emails_mx(emails):
4038:def _get_host(url):
4047:def _is_junk_result(title, url):
4062:def _extract_company_name(title, url):
4088:def _get_country_from_url(url):
4098:def _get_short_description(snippet):
4111:def _parse_results_to_suppliers(all_results):
4173:def _is_cis_email(email: str) -> bool:
4182:def _filter_emails(emails):
4205:def _select_rfq_emails(emails, max_per_domain=2):
4271:def _collect_domains_from_results(all_results):
4295:def _inject_emails_into_suppliers_text(suppliers_text, emails):
4361:def _inject_scraped_emails(suppliers_text, found_domains, snippet_emails):
4390:def _enrich_suppliers_emails(suppliers_text, max_queries=4):
4469:def _format_suppliers_for_docx(suppliers, valid_emails):
4501:def _extract_suppliers_from_search_results(subject, brand, china_available, all_results):
4618:def search_and_format_suppliers(subject, brand=None, mode="global", china_available=False):
4716:def extract_markdown_table(text):
4779:def mine_spec_table(response):
4816:def is_brand_trusted(brand, response, pre_brand=""):
4840:def add_key_value_table(doc, content):
4858:def add_single_column_table(doc, content):
4868:def add_structured_report(doc, response_text):
4947:def _is_service_line(line):
4953:def _clean_suppliers_for_docx(suppliers_text):
4995:def create_tender_report(reg_number, deadline_str, response, suppliers_text, output_dir):
5038:def _load_sender_config():
5051:def get_sender_for_region(region_str, sender_config):
5066:def resolve_sender_credentials(sender_id, sender_config, secrets=None):
5075:    email = secrets.get(cfg.get("email_secret", ""), "")
5076:    password = secrets.get(cfg.get("password_secret", ""), "")
5097:def init_email_db():
5116:def log_email(tender_num, tender_subj, to_email, status, error_msg=""):
5135:def already_sent(tender_num, to_email):
5148:def check_mx(domain):
5159:def extract_emails_from_text(text):
5172:def load_email_template(template_path=None):
5196:def send_rfq_email(to_email, tender_data, from_email=None, from_password=None, template_path=None):
5208:        return False, "Email credentials не заданы (проверьте secrets.txt и sender_config.json)"
5351:def load_queue():
5360:def doc_still_exists(doc_path):
5387:def _field_is_blank(value):
5395:def validate_tender_fields(entry):
5431:def _check_placeholder(value):
5432:    """True если значение выглядит как незаполненный placeholder из secrets.txt."""
5440:def validate_sender_credentials(sender_id, sender_config, secrets):
5449:    email_key    = cfg.get("email_secret", "")
5450:    password_key = cfg.get("password_secret", "")
5456:        warnings.append(f"'{sender_id}': email_secret не задан в sender_config.json")
5460:            f"placeholder — замени реальным адресом в secrets.txt"
5464:        warnings.append(f"'{sender_id}': password_secret не задан в sender_config.json")
5468:            f"placeholder — замени реальным паролем приложения в secrets.txt"
5476:def _extract_emails_from_docx(path):
5522:def _extract_deadline_from_docx(path):
5538:def _get_tender_meta_from_db(reg_number):
5569:def _format_date_english(date_str):
5578:def _extract_warranty_requirement(text):
5603:def _extract_brand_from_docx(path):
5620:    def _elem_text(el):
5654:def _format_items_table_as_text(items):
5662:    def _qty_str(item):
5682:    def _fmt(row):
5692:def _extract_items_table_from_docx(path):
5711:    def _elem_text(el):
5714:    def _col_idx(headers, keywords):
5769:                def _get(idx, default=''):
5784:def build_queue_from_reports(folder_arg, force=False):
5911:def send_rfq_main():
5934:        print("  ❌ Отправка прервана — заполни secrets.txt и повтори запуск.")
6021:                  f"(проверь secrets.txt)")
6094:def _build_reliable_hints(row, excel_subject, excel_nmck, excel_customer, excel_region):
6109:def extract_brand_from_subject_and_filenames(excel_subject, downloaded_paths):
6183:def _enrich_brand_in_response(response, brand_hint, doc_text, excel_subject,
6230:def process_tender(row, idx, total, incomplete_list, rfq_queue, sender_config=None):
6520:def _parse_force_args():
6544:def _parse_retry_errors_args():
6561:def _clear_for_retry(nums):
6588:def main():
